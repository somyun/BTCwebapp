# Codex session attachments
.codex-remote-attachments/

# Contains an embedded Firebase service-account private key.
# Keep it locally for the existing Apps Script project, but never commit it.
apps-script/firebase_key.js

# Local CAD conversion and validation workspace. Only cad-data/ is deployable.
.analysis/
호포도면지도_실행/
